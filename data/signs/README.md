# Per-sign assets

This directory is intentionally a folder (not a file) so branches can add
individual sign metadata/files here without triggering a file-vs-directory
merge conflict at `data/signs`.
